#include "pushSwap.h"

int main(int argc, char *argv[])
{
	if (argc == 1)
		return (write(1, "Arg error\n", 10));
	//check num array
	//make stack
	//serch rule
	write(1, argv[0], 10);
	write(1, "\nHello push swap\n", 17);
	return (0);
}